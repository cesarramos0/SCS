# scs-backend

Base skeleton for a modular FastAPI backend (MVC-like).

## Python version
- 3.10

## Quick start
```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
uvicorn main:app --reload
```

